# PC 전원 On/Off 실시간 모니터링 기능 개선 작업 지침서

- 대상 파일: `main.js` (Electron 기반 출퇴근 기록 앱, v1.0.32)
- 작성 목적: AI(Claude 등)에게 그대로 전달하여 코드를 수정·보완시키기 위한 지침
- 핵심 문제: 직원이 컴퓨터 전원을 종료해도 "퇴근(PC 끈 시간)"이 실시간으로 반영되지 않고, 이전 기록이 그대로 남는 현상

---

## 1. 현재 코드 구조 요약

| 구성 요소 | 역할 | 위치(함수) |
|---|---|---|
| `sendSyncRequest` / `sendSyncShutdownRequest` | 서버(GAS)로 출퇴근 시간 전송 | 54~171행 |
| `syncEventLogs` | Windows 이벤트 로그(부팅/종료) 전체 분석 후 서버 반영 | 201~334행 |
| `syncLastShutdownTime` | 부팅 직후 과거 미전송 종료시간 역추적 | 341~376행 |
| `startShutdownWatcher` | PowerShell로 종료 이벤트를 실시간 감시 | 382~510행 |
| `startHeartbeat` / `sendHeartbeat` | 60초마다 "현재시간=offTime"으로 전송 (실시간 반영의 핵심 안전장치) | 532~564행 |
| `powerMonitor.on('shutdown'/'suspend'/'resume')` | Electron 자체 전원 이벤트 감지 | 653~678행 |
| `app.on('session-end')` / `app.on('before-quit')` | OS 세션 종료 시 최종 전송 + 종료 지연 | 765~825행 |
| `process.on('SIGTERM'/'SIGINT'/'exit')` | 프로세스 강제 종료 대비 | 689~723행 |

즉 "실시간"을 위해 이미 **6중 방어 장치**(watcher, powerMonitor, session-end, before-quit, 프로세스 시그널, heartbeat)를 마련해 두었습니다. 문제는 장치가 부족한 게 아니라, **핵심 5개 장치가 전부 "종료되는 그 순간에 네트워크 전송을 완료해야 성공"하는 동일한 약점을 공유**한다는 것입니다. 예외적으로 heartbeat만 "종료 전에 미리" 보내는 방식이라 성격이 다릅니다.

---

## 2. 근본 원인 분석

### 2.1 "종료 순간 전송" 방식의 구조적 한계 (가장 중요)
Windows는 앱 종료 시 기본적으로 약 5초의 유예시간만 부여합니다(`WaitToKillAppTimeout`). 그 안에 다음이 모두 끝나야 기록이 남습니다: DNS 조회 → TLS 핸드셰이크 → 구글 앱스스크립트(GAS) 콜드스타트 실행 → 응답. GAS는 특히 콜드스타트 시 수 초가 걸릴 수 있어, 이 짧은 유예시간 안에 못 끝나면 **기록 자체가 통째로 유실**됩니다. 이것이 "종료했는데 반영이 안 되고 예전 기록이 남는" 현상의 가장 유력한 원인입니다.

### 2.2 session-end + before-quit 중복 지연
- `session-end`(801행)에서 recordOff 전송 후 **2초** 지연 후 `app.quit()`
- 그런데 `shutdownHandled=true`가 이미 되어 있어도, `before-quit`(765행)의 `if (isOsShutdown && !isDelayingQuit)` 조건이 다시 걸려 **3초** 추가 지연

→ 합산 최대 5초 가까이 종료가 지연되며, 이는 Windows의 강제종료 유예시간과 거의 맞닿아 오히려 **앱이 통째로 강제 종료되어 전송이 중간에 끊길 위험**을 키웁니다. 이는 명백한 로직 중복이며 수정 대상입니다.

### 2.3 이벤트 ID 6006 기반 실시간 감시의 태생적 한계
`startShutdownWatcher`(382행)가 감시하는 이벤트 중 6006("이벤트 로그 서비스 중지")은 **종료 절차의 아주 마지막 단계**에 기록됩니다. 이 시점엔 감시용 PowerShell 프로세스 자신도 이미 종료 대상이 되어 있을 가능성이 높습니다. 즉 이름은 "실시간 감시"지만, 구조상 정작 필요한 순간에 못 잡을 확률이 상존합니다. 참고용 보조 수단일 뿐, 유일한 실시간 수단으로 신뢰해서는 안 됩니다.

### 2.4 Heartbeat(60초) 자체는 좋은 방향이나 주기가 김
`startHeartbeat`는 "종료 시점을 잡으려 하지 않고, 평소에 미리 생존신호를 보내 두는" 방식이라 2.1의 약점에서 자유롭습니다. 다만 현재 60초 간격이라, 정확히 60초 전 기록이 최신값이 될 수 있어 "실시간"이라 부르기엔 오차가 큽니다.

### 2.5 서버(GAS) 측 로직은 main.js만으로 검증 불가
직원 스마트폰 화면에 표시되는 "이전 기록"이 정확히 무엇을 기준으로 갱신/미갱신되는지는 **GAS(Google Apps Script) 코드**를 봐야 확정할 수 있습니다. 예를 들어 `isHeartbeat=true` 파라미터를 서버가 실제로 어떻게 처리하는지, 최신 시간이 아니면 무시하는 로직이 있는지 등은 main.js에 없습니다. **이 지침서만으로 100% 해결을 보장할 수 없으며, GAS 스크립트 원본도 함께 검토가 필요합니다.**

### 2.6 (운영 이슈, 코드 문제 아님) Windows 빠른 시작
'빠른 시작(Fast Startup)'이 켜져 있으면 실제로는 종료가 아니라 하이버네이션에 가깝게 동작해 6006/1074 이벤트 자체가 지연되거나 기록되지 않을 수 있습니다. 코드로 해결할 수 없는 부분이라 운영 가이드로 별도 안내가 필요합니다.

---

## 3. 개선 방향 (설계 원칙 전환)

> **"종료되는 순간을 붙잡는다" → "평소에 자주 생존신호를 보내고, 신호가 끊기면 마지막 신호 시각을 잠정 퇴근시간으로 본다"**

- 종료 시점 push(watcher, powerMonitor, session-end 등)는 "성공하면 좋은 조기 신호"로 격하하고 유지
- Heartbeat 주기를 대폭 단축해 "사실상 실시간"에 가깝게 만드는 것이 가장 확실하고 통제 가능한 개선책
- 부팅 시 이벤트 로그(6006/1074) 기반 사후 보정은 그대로 유지 → "잠정치"를 "정확치"로 나중에 덮어쓰는 역할

---

## 4. AI에게 지시할 구체적 작업 목록 (우선순위 순)

### [필수-1] Heartbeat 간격 단축
- 위치: `startHeartbeat` 함수, 542행
- 현재: `setInterval(() => sendHeartbeat(name), 60000);`
- 수정 지시: 60000(60초) → **15000~20000(15~20초)**로 변경
- 주의: 전송 빈도가 3~4배 늘어나므로, GAS 스크립트 쪽에서 동시성 오류(스프레드시트 Lock 충돌)가 나지 않는지 서버 코드도 함께 점검 필요 → **GAS 스크립트 원본 확보 후 재검토 필수**

### [필수-2] session-end / before-quit 중복 지연 제거
- 위치: `before-quit` 핸들러 791~798행, `session-end` 핸들러 801~825행
- 수정 지시: `session-end`에서 이미 `recordOff`를 전송하고 `shutdownHandled=true`로 만들었다면, `before-quit`의 추가 3초 지연 로직은 실행되지 않도록 조건을 강화할 것. 즉 "아직 아무도 전송을 시도하지 않은 경우"에만 `before-quit`가 지연을 걸도록 하고, 전체 지연 시간 합계는 **최대 2~3초 이내**로 제한.

### [필수-3] 전송 성공/실패 이력을 파일로 영속화
- 현재: `console.log`/`console.error`만 사용 → 앱 재시작·종료 시 즉시 소실되어 "왜 실시간 반영이 안 됐는지" 사후 확인 불가
- 수정 지시: 모든 전송 시도(성공/실패, 시각, 어떤 방식으로 시도했는지)를 `app.getPath('userData')` 하위의 로그 파일에 append 방식으로 기록. (예: `electron-log` 패키지 사용 또는 자체 `fs.appendFileSync` 구현)
- 목적: 이후 유사 사례 발생 시 "정말 네트워크가 늦었는지 / 애초에 전송을 안 했는지"를 바로 확인 가능하게 함

### [권장-4] 서버(GAS)·대시보드 측 "최종 하트비트 시각 기반 부재 판정" 로직 도입 요청
- main.js 범위를 벗어나지만, 실시간성 확보의 핵심입니다.
- 요청 내용: GAS가 매 하트비트 수신 시 "마지막 수신 시각"만 갱신 저장하고, 대시보드(웹/모바일)가 해당 값을 표시할 때 "마지막 확인 시각으로부터 (하트비트 주기 × 2) 이상 경과 → PC 꺼짐으로 추정 표시"하도록 **대시보드 쪽에서 판정**하게 할 것. 이렇게 하면 PC가 실제로 전송에 실패해도 대시보드가 "언제까지는 확실히 켜져 있었다"를 즉시 보여줄 수 있어 체감 실시간성이 크게 올라감.
- 필요 자료: 현재 GAS 스크립트 원본, 대시보드(`fitsociety-hue.github.io/pconoff`) 소스코드 → **추가로 확보해서 함께 전달 필요**

### [권장-5] 종료 워쳐(watcher)의 역할 재정의
- 위치: `startShutdownWatcher` 함수 및 관련 주석
- 수정 지시: 코드를 제거할 필요는 없으나, 주석에 "이 워쳐는 성공 시 조기 신호일 뿐이며, 실시간성의 최종 보장 수단은 heartbeat"라고 명시. 향후 유지보수자가 이 워쳐를 유일한 실시간 수단으로 오인하지 않도록 함.

### [운영-6, 코드 아님] Windows 빠른 시작 비활성화 안내
- 전 직원 PC에서 '제어판 → 전원 옵션 → 빠른 시작 켜기' 항목을 끄도록 관리자 공지. (코드 수정과 무관, 운영 절차)

---

## 5. 수정 후 검증 시나리오

1. **정상 종료**: 새 하트비트 주기(15~20초) 이내에 대시보드 "퇴근(PC 끈 시간)"이 갱신되는지 확인
2. **강제 종료**(전원버튼 길게 누름): 마지막 하트비트 시각까지는 반영되고, 다음 부팅 시 6006/1074 이벤트로 정확한 시각이 사후 보정되는지 확인
3. **네트워크 단절 중 종료**: `shutdown_cache.json`에 저장되고, 재부팅 후 `retryCachedShutdown`으로 재전송되는지 확인
4. **절전 모드 진입/복귀**: `suspend`/`resume` 처리 후 이상 없는지 확인
5. **연속 재부팅**(설치/업데이트 등): `syncEventLogs`의 "재부팅 vs 최종 종료" 판별 로직이 오탐하지 않는지 확인

---

## 6. AI에게 전달 시 사용할 수 있는 예시 프롬프트

```
첨부한 main.js에서 아래 사항을 수정해줘.
1) startHeartbeat 함수(542행)의 heartbeat 간격을 60000에서 15000으로 변경
2) before-quit 핸들러(765~798행)와 session-end 핸들러(801~825행)의 중복 지연을 제거하고,
   전체 지연을 최대 2~3초로 통합
3) 모든 console.log/console.error 전송 로그를 app.getPath('userData') 하위 로그 파일에도
   함께 기록하도록 파일 로깅 기능 추가
수정된 전체 코드를 보여줘.
```

---

## 7. 유의사항 (중요)

- 본 지침서의 4번 항목 [권장-4]는 **GAS 스크립트와 웹 대시보드 코드 없이는 실제 구현이 불가능**합니다. 실시간성을 근본적으로 개선하려면 해당 코드도 함께 확보하여 AI에게 전달하시길 권장합니다.
- [필수-1]로 하트비트 주기를 단축하면 서버 호출 횟수가 늘어나므로, GAS 무료 실행 할당량(quota)이나 스프레드시트 동시 쓰기 충돌 여부도 함께 점검해야 합니다.
