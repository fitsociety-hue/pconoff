# 실시간 PC 종료 감시 로직 분석 및 구현 보고서

## 1. 실시간 종료 감시 로직 개요
본 앱의 가장 핵심적인 기능인 "실시간 종료 감시 로직"은 직원이 PC를 종료하는 즉시(예: 17시 30분 29초) 그 시간을 정확히 감지하여 초 단위까지 실시간으로 서버에 전송하는 기능입니다. 6월 30일 18시 10분 이후 업데이트된 로직을 분석한 결과, 여러 가지 비동기 이벤트와 OS 강제 종료 상황을 극복하기 위해 다중 방어 시스템(Multi-layered Detection)으로 구현되어 있습니다.

## 2. 핵심 분석 내용

### A. Electron 내장 이벤트를 통한 실시간 감지
- **`powerMonitor.on('shutdown')` 및 `app.on('session-end')` 활용:** OS가 종료 시그널을 보내면 즉시 이를 감지합니다. Node.js 프로세스가 OS에 의해 강제 종료되기 전에 가로채어 실시간 시간을 기록합니다.
- **Detached Process (독립 프로세스) 전송:** 종료 시 앱이 꺼지면서 HTTP 통신이 끊기는 문제를 방지하기 위해, Node.js에서 분리된(detached) 백그라운드 `curl.exe` 또는 `powershell.exe` 프로세스를 생성하여 서버로 요청을 던집니다. 이를 통해 PC 전원이 꺼지는 과정 중에도 백그라운드에서 안전하게 통신이 완료됩니다.

### B. 백그라운드 PowerShell 이벤트 로그 실시간 감시 (Watcher)
- **`System.Diagnostics.Eventing.Reader.EventLogWatcher` 활용:** Electron 앱 이벤트뿐만 아니라, Windows 내부 시스템 수준에서 발생하는 종료 이벤트(Event ID 1074, 42, 6006 등)를 백그라운드 PowerShell이 실시간으로 감시(Watch)합니다.
- 종료 버튼을 누르면 Windows 이벤트 로그에 즉각 기록되는데, Watcher가 이를 감지하는 즉시 1초의 지연도 없이 해당 시간(예: 17:30:29)을 초 단위까지 구글 서버로 직접 전송합니다.

### C. 실패 방지 3중 백업 (과거 기록 역추적)
- 실시간 통신 당시 네트워크 지연이나 전원 차단으로 인해 전송이 실패했더라도, 다음번 PC를 켤 때(부팅 시) 과거의 정상 종료 이벤트(Event ID 6006) 로그를 역추적(syncLastShutdownTime)하여 서버로 과거 종료 시간을 정확하게 보정하여 전송합니다.

## 3. 재구현 및 기능 보강 (개선 사항)
직원이 17시 30분 29초에 전원을 종료했을 때 스마트폰에서 즉시 29초 단위까지 확인할 수 있는 기능을 더욱 완벽하게 보장하기 위해 코드를 다음과 같이 재구현/보강했습니다.

1. **초 단위 정확도 완벽 보장:** 시간 데이터를 서버로 전송할 때, 잘림(Truncation) 없이 초 단위(ss)를 명확히 포함시켜 실시간성 보장.
2. **실시간 Watcher 통신 Fallback(대비책) 추가:** `curl.exe` 명령어가 특정 Windows 환경에서 실패할 경우를 대비하여, PowerShell Native 명령어인 `Invoke-RestMethod`로 백그라운드에서 즉시 재시도하는 2차 방어 로직을 `main.js`에 추가 적용 완료.

## 4. 결론
이러한 **3중 감지 및 분리형 프로세스 통신(Detached HTTP Request)** 구조 덕분에, 직원이 PC 전원 버튼을 누르거나 시작 메뉴에서 시스템 종료를 실행하는 그 순간의 "초 단위" 시간이 즉시 감지되며 실시간으로 클라우드 서버에 반영됩니다. 사용자는 스마트폰이나 태블릿 등 어디에서든 지연 없이 정확한 퇴근 시간을 확인할 수 있습니다.

해당 로직의 견고성을 더해 새로운 코드를 반영하였으며, 신규 설치 파일(`출퇴근기록앱 Setup`) 빌드를 완료했습니다.
